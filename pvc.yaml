apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: papyrus-pvc
spec:
  storageClassName: topolvm-provisioner
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
